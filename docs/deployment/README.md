# Deployment

For integration development run `docker compose up --build`. The compose password is development-only and must never be used in production. Source authorization and credentials are stakeholder-controlled.
